# module with multiplication, division of polynomials in finite fields


def mult(f: list[int], g: list[int], p: int, h: list[int]) -> list[int]:
    """
    Returns f * g in Z_p[X]/(h)
    """

    return []


def div(
    f: list[int], g: list[int], p: int, h: list[int]
) -> tuple[list[int], list[int]]:
    """
    Divides f by g in Z_p[X] and returns q, r (in this order) such that
    f = q * g + r
    """

    return [], []
