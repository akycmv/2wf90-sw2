# module with extended euclidean algorithm implementation


def egcd(f: list[int], g: list[int], p: int) -> tuple[list[int], list[int], list[int]]:
    """
    Finds a, b, d such that:
    a * f + b * g = d, in Z_p[X],
    where, a, b not both zero polynomial

    and returns a, b, d (in this order)
    """
    return [], [], []
